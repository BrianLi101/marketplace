name: MarketplaceSpike
options:
  bundleIdPrefix: com.brianli101
  deploymentTarget:
    iOS: "17.0"
targets:
  MarketplaceSpike:
    type: application
    platform: iOS
    sources: [Sources]
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.brianli101.marketplace.spike
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        GENERATE_INFOPLIST_FILE: YES
        TARGETED_DEVICE_FAMILY: "1"
        CODE_SIGNING_ALLOWED: "NO"
